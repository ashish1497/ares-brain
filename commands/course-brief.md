---
description: Briefing for the next class of one course — what it covers, what to read, questions to hold.
---

Use the `briefing` skill, Mode A, for the course the user named (match it to a slug from `courses/_index.json` — accept a partial name). If they named no course, list the courses that have an upcoming session (call `brain_next_session` for each slug) and ask which.
