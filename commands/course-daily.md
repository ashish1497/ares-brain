---
description: Run the morning routine now — scrape, calendar sync, write today's brief, notify.
---

Invoke the `mesa-course-agent:course-daily` skill. This is the same routine the 6 AM
launchd job runs; running it by hand just regenerates today's `daily/<date>.md`.
