---
description: Build or refresh the revision pack (drill.json) — map, cheatsheet, frameworks, flashcards, MCQs — for one course.
---

Use the `ares-brain-drill-build` skill for the course the user named (match to a slug from `courses/_index.json`, accept a partial name). If none named, run `brain_status` with no course, show which courses have `drillSourcesBehind > 0`, and ask which to build.
