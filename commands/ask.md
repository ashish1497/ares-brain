---
description: Ask a question about one course; answered from its material with citations.
---

Use the `ask` skill. The user's message after the command is `<course> <question>` — first token(s) are the course name (match to a slug), the rest is the question.
