# C-tasks progress ledger

Plan: docs/superpowers/plans/2026-09-03-c-tasks.md
Branch: c-tasks    Base: main @ 2637949


- Task 1: complete (commit b72e907, review clean — approved, Minors only). brain.get_doc (traversal-safe) + brain-get subcommand + brain_get MCP tool. pytest 153, vitest 20. brain.py imports still clean.
  Deviations (both sound): fixture updatedAt quoted (matches real A-ingest _yaml_scalar output); shared brain_corpus fixture gained book-lean-startup.md -> bumped 3 corpus-size assertions 6->7 + added "book" to a type set (not loosened).
- Task 2: complete (commit 0704ed4, review clean — approved, Minors only). study_dir/write_study_artifact/list_study + brain-write-study subcommand + brain_status.studyArtifacts. pytest 158, vitest 20. imports clean.
- Task 3: complete (commit 39ca1cb, markdown-only, verbatim to approved plan; suites unchanged 158/20). ask skill + /ask command.
- Task 4: complete (commit db653e4, markdown-only, verbatim to plan; suites 158/20 unchanged). assignment-help skill + command.
- Task 5: complete (commit d4f6057, markdown-only, verbatim to plan; suites 158/20 unchanged). testprep skill + command.
- Task 6: complete (commit f75b496, review clean — approved, Minors only). book-summary skill + command + _normalize_study_book_summaries ingest rule + fixture + README + live run. pytest 159, vitest 20, build ok.
  LIVE ROUND-TRIP VERIFIED: study/book-*-summary.md -> ingest -> normalized/book-summary-*.md -> brain-index finds it via type:book-summary query -> delete study file -> re-ingest -> orphansDeleted:1, doc gone, query [].
  Minors -> final: ingest.py now has 2 frontmatter splitters (new parse_frontmatter + _normalize_transcripts inline) - could consolidate; report's "no yaml" rationale was false (yaml IS in venv) but code is fine; title fallback is stem.replace("-"," ") not bare stem.

ALL 6 C TASKS COMPLETE. pytest 159, vitest 20, tsc/build clean. Ready for whole-branch review.

## C-tasks whole-branch review — DONE
- Review (opus): 1 Critical + 7 Important + minors. Fix subagent → 187f410. Re-review (opus): all fixed, 1 trivial hygiene blocker (committed node_modules) → 9c956be. Ready to merge.
- Deferred follow-ups (non-blocking): (1) ingest.parse_frontmatter regex reader mishandles quotes/unicode vs yaml — make it use yaml; folds in the two-splitter consolidation. (2) confinement guards resolve after following a symlinked study/ or normalized/ dir — tighten to repo-containment. Both filed for post-merge.
- suites @ 9c956be: pytest 165, vitest 21, tsc + build clean.

## D-oauth — branch d-oauth off main (842f025). Plan: docs/superpowers/plans/2026-09-03-d-oauth.md (one task).
Task 1: NOT started. Step 9 (browser consent) needs user + a real Desktop client_secret.json (current one is "web" → do_auth rejects it correctly).

## D-oauth — MERGED to main @ 58a879b (2026-09-04)
Task 1 done (68a5b52), reviewed (sonnet, approved, Minors only), 1 Minor fixed (3d94e9e: do_auth non-object JSON guard). pytest 173, vitest 21, tsc/build clean. Branch deleted.
OPEN user-assisted: swap repo-root client_secret.json ("web") for a Desktop-type OAuth client, then `cd scraper && uv run python lms_scrape.py calendar-auth` (browser consent), then live calendar-sync verification. Commands in docs/lms-api.md "D-oauth live run — PENDING".

## D-oauth live run — DONE (2026-09-04)
User authed via Desktop client. calendar-sync live: created 7, 2nd run unchanged 7, 0 errors. Idempotent end to end. D fully shipped + verified.

## D-sessions — MERGED to main (2026-09-04). Class schedule + program events on the calendar.
Task 1 (a9cf1ef) + review (sonnet, approved) + stray .token.lock untracked. Merged.
Live: created 142 + updated 7 (canonical location slot), 2nd run unchanged 149, 0 errors. 94 sessions + 48 events + 5 exams + 2 asg. pytest 177.

## E — daily job. Branch e-daily off main. Spec .../specs/2026-09-04-e-daily-design.md, plan .../plans/2026-09-04-e-daily.md (5 tasks).
T1 daily_brief.py+CLI  T2 MCP tool  T3 course-daily skill+cmd  T4 /daily-setup+/daily-uninstall  T5 live run+docs.
(The commands/calendar-sync.md gcloud-hint fix is in the diff base 8e0914d — already on main via an earlier branch, NOT an e-daily commit.)
T1: NOT started.

E T1: complete. daily_brief.py + CLI (3d5518c) + review (sonnet, needs-work) + fixes (3b6701e: resilience/dedupe/minors). pytest 188. Reviewer's 3 approved deviations kept (datetime _parse, _prereads_for title-session match, now.timestamp staleness).

E T2: complete. daily_brief MCP tool (e4fee67), verbatim from brief. vitest 24. No per-task review (pure transcription of reviewed+corrected plan code).

E T3: complete. course-daily skill + /course-daily command (e769e91), verbatim from brief. Markdown; live-verified in T5.

E T4: complete (1dfa64e) — /daily-setup + /daily-uninstall, verbatim.
E T5: daily-brief CLI live-verified (4 classes, title-preread match worked, scrapeStale false); digest+notify hand-run. launchd/plugin end-to-end = USER step (plugin not installed here). Docs + README updated (862e97c).
ALL 5 E TASKS DONE. pytest 188, vitest 24, tsc/build clean. Ready for whole-branch review.

E whole-branch review (opus): "With fixes" — no Critical, 3 Important + minors. Fixed d2c5d51 (scrapeStale _STALE_HOURS constant; plist --allowedTools split Bash(cd:*)+Bash(uv run...); skill pre-read path composition; per-dir try/except; stderr traces). pytest 189, vitest 24. Ready to merge.
## F — dashboard. Branch f-dashboard off main. Spec .../2026-09-08-dashboard-design.md, plan .../2026-09-08-dashboard.md (5 tasks).
T1 python transcribe-url+--inbox  T2 dashboard/server  T3 dashboard/web  T4 glue+CI  T5 live run.
T1: NOT started.
T1: complete (3e3369b, review Approved). Minors deferred: _write_url_transcript omits course: key (harmless, ingest overrides); test_cli_transcribe_url_dispatch mocks transcribe_url so arg-threading untested; whisper_raises test does not pin error string.
T2: complete (b20e1d6 + fixes a21cdf5). Review Needs-work -> 3 Important fixed (upload path-traversal, SSE leak/crash, Host/Origin guard) + minors. server vitest 25.
T3: complete (39a7055 + fixes 3600fb1). Review Approved -> fixed 2 freeze bugs (reload-mid-job, SSE drop), Tabler webfont, --color-accent, minors. web vitest 6.
DEFER to whole-branch: Tabler webfont adds ~4MB to dist/ (ttf 2.8MB) - subset or self-host 6 glyphs if it matters. dashboard/web has no tsc typecheck (T4 should add to check + CI).
T4: complete (05cb0d6). Glue: dashboard/ aggregator workspace, root dashboard scripts, /mesa:ares-brain-dashboard command, CI dashboard job, eslint dashboard/** glob + web tsc check. npm run check CLEAN (0 errors, 10 scoped no-explicit-any warnings). Pre-commit hook ran normally.
T5: live run done. UI + job runner + SSE + guards (409/403) verified in browser. transcribe-url NOT verified e2e (yt-dlp cannot reach YouTube from this network). FINDING for fix wave: transcribe-url CLI exits 0 on {ok:false} -> dashboard shows failed transcribe as done. ALL 5 TASKS DONE. Ready for whole-branch review.
Whole-branch review (opus): With fixes - no Critical, 4 Important. Fixed e3f9840 (CLI exit-code on failure; vite proxy changeOrigin - primary launch path was 403ing; ../. slug rejection; course-list refresh) + minors. Live-verified npm run dashboard proxy POST -> 202. pytest 202, server 28, web 7, check clean. MERGED.
Deferred follow-ups: Tabler webfont ~4MB (subset later); dark mode not visually verified; .dark toggle + inbox _done/ move not built (spec extras).

## G — outcome dashboard. Branch g-dashboard off main. Spec .../2026-09-08-outcome-dashboard-design.md, plan .../2026-09-08-outcome-dashboard.md (5 tasks).
T1 overview.py+CLI  T2 /api/overview + transcribe job kind  T3 web React+neobrutalism.dev migration  T4 the 10 sections  T5 glue+live.
T1: complete (4bc18e5 + fixes 9ac1f2c, review Approved w/ follow-ups -> all applied). overview.py data layer + `overview` CLI subcommand. Fix wave: attendance 3-state (state ok|watch|risk, atRisk kept as state!="ok"), per-row guard in attendance loop + defensive int(), _name_match word-boundary weight matching (Pitch 1 != Pitch 10), _SECTION_RE two-pipe format, _INLINE_RE keeps (), nested-breakdown -> parent total, testprepExists scoped to exam's own course. Spec §3+§5 updated. pytest 225, vitest 24.
T2: complete (869ae5e, self-reviewed clean - verbatim from brief). GET /api/overview -> runPythonJSON(["overview","--json"]) 200|503; runPythonJSON buffered helper in lib/python.ts (15s timeout, last-{-line parse, never throws); "transcribe" job kind (transcribe --course <slug> --json, no --inbox) + KINDS + course-required. server vitest 28->31. check clean.
Real overview shape saved: .superpowers/sdd/2026-09-08-outcome-dashboard/overview-shape.txt + overview-sample.json (spec §3 jsonc drifted slightly - T3+ write Overview type from the sample).
T3: complete (f7bf4a7 + nullable-kpi fix 243d8ea). Preact->React (react 18 + @vitejs/plugin-react), VENDORED neobrutalism kit (shadcn init interactive + registry token-conflict -> hand-wrote card/button/badge/alert/progress/table/skeleton in src/components/ui/ w/ CVA+cn, 3px border + 4px offset shadow + 2px radius + palette B). Full Overview type in src/api.ts from real sample. Blank App (raw JSON dump). Deleted old Preact app/components/tests. web vitest 7->3 (JobLog + api x2). Env notes: this npm (v11.19) drops vite-peer devDeps on plain install - lockfile regen needs --legacy-peer-deps once; CI fine. JobLog ported but still on DEAD old tokens -> T4 retargets.
T4: complete (5bab5bc). 13 components + real App.tsx + JobLog retarget (no dead tokens). web vitest 3->37 (15 files + factories.ts). check clean.
T4 review (sonnet): SPEC OK, quality approved. 1 Important (missing-book upload dropzone not built - only static hint) + minors. Fix wave dispatched (a25f1b5cccb965aa9): (1) real upload dropzone in GapsCard -> upload(mentionedIn[0],"book",files), (2) "Scrape is N days old" text + ageHours prop, (3) CopyButton .catch + note only on success, (4) App.tsx error state + Retry (brief dropped it -> infinite loading on API fail).
T4 review DEFERRED: skeleton loading rows (design §6 wants them, brief omitted - low value for <1s localhost load); duplicate Sync button Header+GapsCard (both in design); dark-mode visual check -> T5.
T4 fix wave complete (c6be637). Upload dropzone in GapsCard, "Scrape is N days old", CopyButton .catch, App.tsx error+Retry. web vitest 37->45.
T5: complete. Live-fix wave (5a64210): index.css theme fix (dark @media{:root} not @media{@theme} + --color-ink + body color), TodayCard fmtTime, WeekCard day-grouping+dedupe+past-filter, attendance_runway sessions_to_end==0 branch, exam courseNames, assignment phrasing. Then controller re-verified light+dark in browser + one-miss floor (8ec80bb: watch one-miss projection gated on conducted>=4 so 2/2=100% early isn't flagged; +2 tests) + README Dashboard section rewrite + docs/lms-api.md G live-run notes. pytest 229, web vitest 49, server 31, check clean.
ALL 5 G TASKS + fix waves DONE. Ready for whole-branch review. Branch g-dashboard, base main, HEAD 8ec80bb.

## G2 — dashboard UX redesign. Branch g-dashboard (continues, NOT yet merged). Spec .../2026-09-09-dashboard-redesign-design.md, plan .../2026-09-09-dashboard-redesign.md (6 tasks).
User verdict on the shipped G build: "genuinely bad UX" - narrow column, no eagle view, no refresh affordance, middot separators, flat visual weight.
Redesign: tabbed shell + sticky summary bar (Re-sync + gear), Today-first landing, focused single-assignment page, full-width max-1400, 4px spacing scale, hairline/3px-rule dividers (NO middots), colour theory (edge=structural, cta=yellow primary-action-only, ok/soon/bad status-only), theme picker in Settings (Meadow green / Ultraviolet violet+cream+yellow x System/Light/Dark, localStorage, data-theme+data-appearance).
Whole-branch review of the pre-redesign build (opus): "Merge with fixes" - 4 must-fix (all overview.py: exam countdown UTC/local off-by-one; build_overview raises on _attendance {"raw":null}; gradePicture done never False + weight matches nothing on real data -> everything green + "100% ahead"; testprepCommand ships literal <course>) + 16 follow-ups. Must-fix + ~8 follow-ups fold into G2 T1.
T1: overview.py data fixes + assignment enrichment (instructionsText/isGroup/cutoffAt/allowLate/materials/sessionRef/prereadPaths) + changed name-resolution + server calendar-sync job kind + runPythonJSON unit test + Host guard on GET /api/overview. NOT started.

G2 T1: complete (1441665). overview.py: local-date countdown (exam off-by-one fixed, nextExamInDays 11->10 in IST), att_list {"raw":null} guard, grade done tri-state + _component_matches keyword category matching + _assignment_backed, _name_match end-of-string fix, risk /50 + overdue floor 0.5, testprepCommand interpolated + testprepCommands[], testprepExists all(), parse_assessment []-on-no-header, _safe label. Enrichment: _html_to_text, _session_ref, assignments +instructionsText/isGroup/cutoffAt/allowLate/materials/sessionRef/prereadPaths, today.changed -> name-resolved list. Server: calendar-sync JobKind+argv+KINDS, GET /api/overview guardOrigin (403 foreign Host), new python.test.ts (runPythonJSON stubbed-spawn). pytest 229->246, server 31->39, web 49, check clean. Deviations: daily_brief.py +1 line (id in _assignments_due for the join); numbered components (Pitch 1) match by name only (keyword path digit-guarded to not break Pitch1/Pitch10 test); committed 2 pre-existing prettier-dirty plan/spec md. api.ts Overview type NOT touched (T2 owns web).
G2 T2: shell. NOT started.
G2 T2: implementer DONE (6a3f1ca). Shell: index.css theme matrix (@theme meadow-light + runtime :root overrides x2 for dark), lib/{theme,useHashRoute,format,types}, Header/Settings/Tabs/SectionHeader/Rule/MetaRow/StatePill, 6 placeholder tabs, App shell + ErrorCard/StaleBanner, JobLog+CopyButton retint, api.ts Overview type finished, 11 old components + their tests deleted, factories.ts reshaped. web 49->52, server 39, check green. Task review pending.
Ruling: ledger stays at .superpowers/sdd/progress.md (this project's committed convention across all groups), not the skill's per-plan path — one continuous record. Cost if wrong: none, only path convention.
Ruling: every tab component gets the uniform superset props {ov,onJob,busy,go,params} (brief step 12 passed per-tab subsets). Cost if wrong: T3-T5 destructure props they don't use; trivial.
Ruling: CopyButton success note edge->ink-muted; JobLog brief text stale (no dead surface tokens, real fix = drop the middot). Cost if wrong: cosmetic.
Ruling: lucide-react removal deferred to T6 (spec §8 follow-up, not in brief). Cost if wrong: one unused dep ships one task longer.
G2 T2 review (opus): Spec compliance ✅ (all 15 steps, 36 hex values match §4.1/§4.2, dark sets byte-identical between explicit+@media copies, @theme trap verified avoided in BUILT css — unlayered :root beats @layer theme, all 6 combos walked, no middots, no §6 leakage). Task quality: NEEDS WORK — 0 Critical, 7 Important:
  I1 theme.test.ts private-mode test is fake (spies Storage.prototype but setup.ts installs an object literal `as Storage`; catch never entered, passes with no try/catch at all) -> spy the instance.
  I2 setup.ts localStorage polyfill installed unconditionally + not a Storage instance -> probe-then-replace, or T4's checklist tests inherit the same blind spot.
  I3 Header r-key has no modifier guard -> Cmd+R/Ctrl+R POSTs a real sync job before reload.
  I4 unknown hash (#foo) renders a blank <main> -> normalize to "today".
  I5 double horizontal padding: outer wrapper px-4/md:px-8 AND Header/Tabs px-4/md:px-8; sticky bars' bg/border stop at 1400px instead of full-bleed.
  I6 --color-rule not in @theme -> divide-rule/border-rule utilities never generated (verified absent from built css); spec §3/§5.3 mandate them, T3-T5 will write them and get a silent no-op.
  I7 Tabs.tsx gap-1.5 (6px) off the 4/8/12/16/24/32 scale.
Ruling: I5 upheld against brief Step 12's snippet — spec §5 wants full-bleed sticky bars; hoist Header+Tabs out of the max-width wrapper, each with its own inner mx-auto max-w-[1400px]. Cost if wrong: one layout revert.
Ruling: I6 upheld against my own "do not add to @theme" instruction — --color-rule goes INTO @theme (placeholder value) so the utilities exist, :root color-mix still supplies the per-theme value. ink-muted/ink-faint stay out (spec/brief only ever use their arbitrary-value form). Cost if wrong: one extra generated utility class.
G2 T2 minors (deferred, for the whole-branch review): onResync/subscribe inline arrows resubscribe listeners every render; App never tears down stop.current on unmount; Settings lacks aria-modal/initial focus/focus trap + no backdrop on mobile (w-full max-w-sm) + bg-black/30 is the only off-token colour; r-key guard misses select/contenteditable; Header chip-tint test doesn't pin the <=7 boundary and getByText("2") is fragile; format.test.ts assumes an English locale; StatePill's VariantProps intersection is redundant; JobLog dot + Tabs count badge use bg-edge as a fill (brief-prescribed).
G2 T2: fix round 1/5 (7 addressed, 0 open; 1a9b902). I1 spy the localStorage instance (proven: test fails with the try/catch removed), I2 probe-then-replace polyfill, I3 metaKey/ctrlKey/altKey guard + test, I4 TAB_IDS allowlist -> unknown hash falls back to today (useHashRoute return shape untouched, deep-link params intact), I5 Header+Tabs hoisted full-bleed with inner mx-auto max-w-[1400px] rows, I6 --color-rule into @theme (built css now emits .border-rule), I7 gap-1.5 -> gap-1. web 52->57. Scoped re-review: all 7 ADDRESSED, no new breakage, dark value-sets still byte-identical.
G2 T2: complete (commit 2330a1e, review clean). The two task commits squashed into one per the task's one-commit requirement (branch unpushed, no upstream — no shared history rewritten). Verified by controller: web build ok, tsc clean, web vitest 57/57 (13 files), server 39/39, root npm run check exit 0. Greps: 0 middots, 0 palette greys, @theme top-level only, 0 off-scale spacing; one opacity-70 left in the untouched vendored ui/card.tsx (T5 deletes it).
G2 T3: Today tab (ClassTimeline, DueNow, ChangedFeed). NOT started.
G2 T2 live verification (controller, real /api/overview on 4319 + vite 5174, viewport 1600x900): all four theme x appearance combos render correctly at RUNTIME — meadow-system under OS-dark resolves to meadow-dark; explicit light overrides OS dark; violet-light and violet-dark both correct; computed --color-edge/paper/card/ink match the spec tables and --color-rule/--color-ink-muted color-mix resolve against the overridden values (the shipped @theme-in-at-rule bug is genuinely gone). Settings: swatches + segmented control + read-only ENVIRONMENT list all present; clicking Meadow swapped the theme LIVE with no reload and wrote both localStorage keys; Esc closes. Hash routing: #assignments, #assignments/sell/123 (deep-link params preserved for T4), #gaps, #today all route; #foo falls back to the Today body. Header shows no middot; the date/term pair is rule-separated; chip tints fire on att 74<75 and brain 0<15 and not on due 2<3 or exam 10>7. Contrast: all 28 spec §4 floors PASS across the four themes (ink/paper 15.4-16.9:1, ink/card 14.6-16.8:1, black on cta/ok/soon/bad 4.65-14.54:1, edge/paper 4.17-8.11:1).
G2 T2 NEW finding from live verification (Important): Header Chip's label span hardcodes text-[color:var(--color-ink-muted)], which overrides the wrapper's `bg-soon text-black` on TINTED chips -> ink-muted composites over the bright soon fill at 1.29:1 (meadow-dark) / 1.21:1 (violet-dark) / 3.22:1 (both lights). The label is invisible in dark themes. Verified in-page (computed colours) and by recomputing the composite offline. Unit tests could not catch it — jsdom has no compositing.
G2 T2 minor from live verification: fmtDate renders "Wed 9 Sept" (en-GB short month) where spec §5.1 states "Wed 9 Sep".
G2 T2 deviation noted (accepted): Settings is a full-height right sheet at desktop width too; spec §5.2 allows "popover anchored to the gear (or a right-side sheet on mobile)". Left as-is.
G2 T2: fix round 2/5 (2 addressed, 0 open; 8e8e273, folded into the single task commit). Chip's label span now gates ink-muted on !soon so a tinted chip's label inherits text-black (contrast 1.21-3.23:1 -> 8.46:1 light / 13.06:1 dark, re-measured in the live page across all four themes); fmtDate builds weekday/month from fixed tables instead of toLocaleDateString (was "Wed 9 Sept" under en-GB, spec §5.1 says "Wed 9 Sep") — parseYmdOrIso's T00:00 trick and the TZ=America/Los_Angeles no-shift test untouched. Independent grep sweep: no other ink-token-on-status-fill anywhere in src (JobLog's ok/bad dots have no text child; its ink-muted pre sits on bg-paper). web 57->60. Scoped re-review: both ADDRESSED, tests revert-sensitive (both fail against pre-fix code), no new breakage.
G2 T2: complete (commit fdf33d5, review clean, 2 fix rounds). One code commit as the task required — the three intermediate commits were collapsed, branch unpushed so no shared history was rewritten.

G2 T3: Today tab (ClassTimeline, DueNow, ChangedFeed). Pre-flight scan vs T2/T4:
  - T3 brief says create `src/components/TodayTab.tsx`; T2 shipped the placeholder at `src/components/today/TodayTab.tsx` (spec §9 layout) and App.tsx imports it from there. Ruling: keep `components/today/TodayTab.tsx`, replace its body in place. Cost if wrong: one import path move.
  - T3 brief Step 3 does `assignments.find(a => a.id === item.id)` but `OverviewDueSoon` has no `id` in the TS type. Verified against the live payload: dueTodayOrTomorrow rows DO carry `id` (T1 added it to daily_brief._assignments_due for the detail join). Ruling: T3 adds `id: string | null` to `OverviewDueSoon` — additive, no other task touches that interface. Cost if wrong: none, the field is real.
  - T3 brief Step 2 wants a pre-read chip with `title="session N pre-read"`, but `OverviewClass` carries no session number (keys: course, courseSlug, start, end, room, instructor, meetingLink, prereadPaths). Ruling: chip label "pre-read", `title` = the joined prereadPaths. Cost if wrong: cosmetic tooltip text.
  - T3 brief Step 3 uses `opacity-60` on submitted rows; the no-opacity rule is about substituting for text tokens, and spec §6.2 itself prescribes row `opacity-80` for club rows. Ruling: row-level opacity allowed on a whole row container, never as a text colour. Cost if wrong: submitted rows read at full strength.
  - EmptyToday's "Next class <time>": `thisWeek[]` items are date-only (`when: ymd`, no clock time), so a time cannot be sourced when today's classes are empty. Ruling: render "Nothing needs you today." plus a next-class line derived from the first future `thisWeek` entry of kind "class" using fmtDate only, and omit the line entirely when none exists. Cost if wrong: the panel loses one sentence.
  - `changed[].counts` kinds seen live: announcement, material, session, assignment, outline — chips need pluralisation. `scrapeAgeHours` is fractional (0.3 live), so the "since" line must handle sub-hour ages.
  Otherwise clean: T3 touches no file T2 owns except the TodayTab body and the additive api.ts field; the `go("assignments/<slug>/<id>")` deep-link shape it emits matches what T2's useHashRoute parses and T4's AssignmentPage will consume.
G2 T3: implementer DONE (04a943b). today/{ClassTimeline,DueNow,ChangedFeed}.tsx + TodayTab body + EmptyToday, api.ts +OverviewDueSoon.id, factories +dueSoon/todayClass + changed counts-key fix, 4 new test files, 3 App.test.tsx fixtures retargeted. web 60->80 (13->17 files), server 39, check green.
G2 T3 review (opus): Spec compliance ✅ (all 5 steps + every §6.1 clause + all 8 pre-flight rulings honoured; sweep clean: 0 cta, 0 ink-faint, 0 middots, 0 gradients, 0 palette greys, 0 off-scale spacing; text on status fills inherits black so the T2 live-round bug class is not reintroduced; App.test.tsx adjustment honest and assertions NOT weakened; factories faithful to the real payload). Task quality: NEEDS WORK — 0 Critical, 8 Important:
  I1 rail dot misses the rail: <ol> has border-l-[3px] pl-4 so content starts 19px right of the rail, and -left-[7px] on a size-3 dot lands its centre ~16.5px off the rail line. The brief's two numbers are mutually inconsistent.
  I2 ClassTimeline "now dot" test asserts only data-state (a test-only attribute fed by the same classifier) — delete every bg-ok/bg-card fill and it still passes.
  I3 DueNow's `Open →` is a raw <button> with no edge focus ring; every other button in the app gets focus-visible:ring-edge from ui/button.
  I4 hoursAway is unbounded below, so an already-overdue item (<0) satisfies `< 6` and renders the amber `soon` pill.
  I5 the `< 6` boundary is untested (3 and 20 only) — `<= 6`, `< 5`, `< 7` would all pass.
  I6 the three Today panels have NO card treatment (bare divs) while EmptyToday gets the full neubrutalist card — populated state reads as unstyled text on bg-paper.
  I7 EmptyToday hand-rolls the exact class string of the shipped ui/card.tsx primitive.
  I8 fmtScrapeAge (a pure time formatter) lives in ChangedFeed.tsx instead of lib/format.ts, so §5.1's Header cannot reuse it.
Ruling: I1 upheld — but fix it by MOVING `pl-4` from the <ol> to each <li> (rail stays border-l-[3px] on the <ol>). Absolute positioning then resolves against the li's padding box, which makes the brief's own `-left-[7px]` correct to within 0.5px. No magic number, no new spacing value. Cost if wrong: one screenshot at T6 shows the dot still off.
Ruling: I4 upheld against the brief's literal `hoursAway < 6` — <0 renders StatePill state="bad">overdue</StatePill> (spec §6.2 uses bad for overdue), 0..6 keeps `soon`. Cost if wrong: overdue items shout red one task early.
Ruling: I6 upheld and it BINDS T4/T5 — every tab section is a card: rounded-nb border-[3px] border-edge bg-card p-4 shadow-[var(--nb-shadow)] md:p-6, via the ui/card.tsx primitive. Spec §3 defines "Card / panel: p-4 md:p-6" and "Card stack inside a tab: space-y-6" (which TodayTab already uses, implying its children are cards) and §4 says the card is told apart by its border + shadow. Consequence: ui/card.tsx is now load-bearing and must NOT be deleted in T5 (the T2 note that it might be is void). Do not use its CardDescription export — it carries an opacity-70 text dim. Cost if wrong: a heavier-looking Today tab and a revert across three tabs.
Ruling: §6.1's "pre-read chip linking to #" stays a non-interactive span in T3. A link to the literal `#` is a dead control, and the course-brief copy-command it is meant to carry is CopyButton work that T4/T5 own. Revisit at T6. Cost if wrong: the chip gains an href one task later.
G2 T3 minors (deferred, for the whole-branch review): index keys where stable ones exist (courseSlug in ChangedFeed, item.id in DueNow whose rows reorder); malformed class start/end yields NaN and silently classifies "past" while fmtTime degrades gracefully; bg-accent + text-black is 3.68:1 on Ultraviolet-light accent #7c3aed (pre-existing, also in ui/button) — T6 contrast pass; TodayTab's next-class .find() relies on ov.thisWeek being sorted ascending (T1 code, unverified); the next-class test never asserts the rendered date and never tests a past class entry, so the `when > ov.date` filter is unguarded; ChangedFeed test asserts chip.tagName === "SPAN"; DueNow test uses native .click() instead of fireEvent.
G2 T3: fix round 1/5 (8 addressed, 0 open; 3e8d607, folded into the single task commit). pl-4 moved <ol> -> <li> so -left-[7px] is right; now-dot test asserts the real fill classes; Open -> routed through Button variant=ghost (gets focus-visible:ring-edge, no bg fill); hoursAway<0 -> StatePill bad "overdue" + dead !=null guard dropped; <6 boundary tests at 6 and 5.9; all three panels + EmptyToday wrapped in the ui/card.tsx Card primitive; fmtScrapeAge moved to lib/format.ts with its tests. web 80->88. Scoped re-review: all 8 ADDRESSED, honesty check on the "2/5/7 pass against both" claim upheld (those were coverage/duplication findings, and the new fill assertions genuinely would catch a deleted class), no new breakage, no scope creep into the deferred minors.
G2 T3 live verification (controller, real /api/overview, 1500x1000, meadow-light + violet-dark): rail dot geometry MEASURED — <ol> padding-left 0px, <li> 16px, dot centre 111.0 vs rail centre 110.5 = 0.5px off, exactly what the ruling predicted (was ~16.5px). All three panels carry border 3px / bg-card / padding 24px (md:p-6) / box-shadow rgb(15,138,95) 4px 4px 0 = var(--nb-shadow) exactly. Class rows show "Delta Classroom | Ananya Narang" rule-separated, and the 14:00 class with no room/instructor shows no leading rule. Due now: rows sorted with the submitted one last, struck, green SUBMITTED pill with black text; "in 19h" for >6h items. Since last scrape: course NAMES, "updated 42m ago" (sub-hour path exercised live, never "0h"), counts pluralised as chips (1 ANNOUNCEMENT / 2 ANNOUNCEMENTS / 7 MATERIALS). Zero middots and zero ink-faint uses in the rendered tab. Not observable live: the now/past dot states (local clock 05:07, every class still future) and EmptyToday (real data is non-empty) — both covered by unit tests with an injected clock/fixture.
G2 T3: complete (commit cbdfbef, review clean, 1 fix round). One code commit as the plan requires — the two intermediate commits collapsed, branch unpushed so no shared history rewritten.
G2 T4: Assignments — list, GradePicture, AssignmentPage, AssignmentChecklist. NOT started. Inherits the T3 ruling that every tab section is a ui/card.tsx Card, and must NOT delete ui/card.tsx.

G2 T4: Assignments — list, GradePicture, AssignmentPage, AssignmentChecklist. Pre-flight scan:
  - Brief says create `assignments/AssignmentsTab.tsx`; T2 already shipped the placeholder there. Ruling: replace in place, keep the TabProps signature (same as T3).
  - Brief Step 3 wants the grade bar's filled segment to be `bg-edge`. That breaks the binding rule that `edge` is structural ONLY and never a fill. ok/soon/bad are status-only, so neither fits a progress fill either. Ruling: use the vendored `ui/progress.tsx` primitive (`value={100 - aheadPct}`) — it already ships `border-[3px] border-edge` + `bg-card` track + `bg-accent` fill, and spec §6.5 independently mandates that same component for the Brain tab's bar, so accent-as-progress-fill is already sanctioned. Binds T5. Cost if wrong: the bar is orange/violet instead of green.
  - Spec §6.2 words the list's status square as "`not-started` = `bad` outline, `draft` = `soon`, `submitted` = `ok` fill", which would make bad/soon/ok border colours. The brief already resolved this to `border-[3px] border-edge` + a bad/soon/ok FILL. Ruling: follow the brief — edge stays the only border colour, status rides the fill. Cost if wrong: a flatter status square.
  - Verified `scraper/overview.py:436` does `assignments.sort(key=lambda x: -x["risk"])`, so the brief's "worst = assignments[0]" is sound and the list needs no client-side sort.
  - Nullability the brief's expressions ignore: `dueAt: string | null` (so `countdown(dueAt)` and the due label need a no-due-date branch), `id: string | null` (a row with no id must not be clickable and cannot be deep-linked), `courseSlug: string | null` (T3's DueNow already guards this for the same hash), `weightPct: number | null` (the header's "worst: {title} ({weight})" needs an ungraded branch). `status` IS non-nullable ("not-started"|"draft"|"submitted").
  - Brief Step 4's `<Panel>` open-codes the card class string. Ruling: it is the T3 Card primitive — that ruling binds here.
  - The brief specifies no empty state for the list. Ruling: render a muted "Nothing open." line inside the card rather than an empty divide-y.
  - T4 is the first task to persist to localStorage. T2's `test/setup.ts` installs an in-memory fallback when native access throws, and on that fallback `Storage.prototype` spying does NOT work — the checklist tests must spy `globalThis.localStorage` (the instance), which is the trap T2's own fix round 1 already hit once.
G2 T4: implementer DONE_WITH_CONCERNS (688e099). web 88->117 (21 files), server 39, check green. Deviations flagged by the implementer: CopyButton gained optional variant/size props (additive, keeps "Brief this session" off the yellow CTA); focused-page panels use a new lightweight PanelLabel instead of SectionHeader.
Ruling (implementer concern 3, resolved from the data): "n open" = ov.assignments.length is CORRECT — scraper/overview.py already `continue`s on submitted assignments, so the array only ever holds non-submitted rows (live: 4 rows, status histogram all not-started). No change needed. Cost if wrong: none, verified against the live payload.
Ruling (new, found while checking that): the brief's "due this week" formula (`hoursAway != null && hoursAway <= 168`) counts OVERDUE items — live data has "Case pre-work" at hoursAway -351.4 (14 days overdue), so the brief's formula yields 3 where the summary bar's chip shows 2. Two contradictory "due" numbers on one screen, and an item 14 days late is not "due this week". Ruling: the list header counts `hoursAway != null && hoursAway >= 0 && hoursAway <= 168`. Cost if wrong: the header undercounts by the number of overdue items, which the list already flags individually with a bad-text countdown.
Note for T6 (payload naming, not a T4 fix): kpis.dueThisWeek is computed at overview.py:570 as `len([d for d in all_due if not d.get("submitted")])` where all_due is due-today/tomorrow — so the field name says "this week" but the value is "today/tomorrow". The §5.1 summary chip inherits that mislabel.
G2 T4 review (opus): Spec compliance ❌ — ruling 10 was NOT implemented (AssignmentsTab.tsx:70 still has the brief's literal `hoursAway <= 168`, so the -351.4h row counts as "due this week": header 3 vs summary chip 2). Everything else in the 6 steps and §6.2 present; rulings 1-9 all honoured and independently spot-verified (0 bg-edge, 0 middots, 0 ink-faint, 0 off-scale spacing, one cta only, no hand-rolled Card/Progress/StatePill, storage test spies the instance). Task quality: NEEDS WORK — 2 Critical, 8 Important:
  C1 AssignmentPage instructionsText.split("\n\n") won't split real payload text: overview.py's _BLOCK_RE emits a SINGLE \n per </p>/</li>/<br>, and only runs of 3+ newlines collapse to \n\n — so a bulleted/multi-paragraph LMS instruction renders as one run-on line; \r\n also unhandled.
  C2 AssignmentChecklist's try/catch guards JSON.parse but not SHAPE — localStorage holding '"hello"' or '{"a":1}' returns a non-array, items.map is not a function, page white-screens on every reload (the bad value stays in storage). readStatus has the same hole and leaves the segmented control with nothing selected.
  I3 <AssignmentChecklist id={id}/> has no key, so a focused-page -> focused-page hash change keeps assignment A's items while id is B, and the next mutation writes A's items into B's key.
  I4 Card is `flex flex-col gap-4`, so every child margin stacks on the gap: the grade break's mt-6 renders 40px, SectionHeader's spec-mandated mb-4 renders 32px, mb-3/mt-3 render 28px. None of 28/32/40 is on the §3 scale. Pattern inherited from T3's panels.
  I5 AssignmentChecklist hand-copies PanelLabel's class string because PanelLabel is un-exported inside AssignmentPage.
  I6 test asserts `className === "font-medium"` exactly — a second class breaks the test guarding ruling 6.
  I7 the checklist checkbox is the only interactive control in the diff without focus-visible:ring-edge.
  I8 quota-exceeded writes fail silently with the UI already updated — item shows added/checked, gone on reload, no signal; its test only asserts "no crash".
  Reviewer verdicts on the two judgment calls: CopyButton's variant/size props ACCEPT (backward-compatible, defaults are the old hardcoded values, and the one-cta-per-view rule makes a demotion escape hatch structurally necessary) but narrow the union; PanelLabel — need real, shape wrong.
Ruling: C1 upheld though NOT yet observable — I checked all four live instructionsText values and they are single-line or empty (no \n at all), so this is latent. Fix anyway (split on /\r?\n\s*\r?\n/ + whitespace-pre-line): the producer demonstrably emits single \n between blocks, so the first course that posts bulleted instructions ships a run-on paragraph. Cost if wrong: none, the fix is strictly more correct on every input.
Ruling: I4 upheld and fixed at the CONVENTION level, T3's panels included — tab-panel Cards take `gap-0`, so children keep the exact margins spec §3 mandates (`pb-2 mb-4` on the header) and nothing double-counts. Chose gap-0 over stripping child margins because §3 states those margins explicitly. Binds T5. Cost if wrong: one class per Card to revert.
Ruling: PanelLabel is deleted and becomes `<SectionHeader rule={false}>` — a `rule?: boolean` prop (default true) that keeps the spec-fixed 11px/tracking-[0.08em] type scale in both modes and swaps the 3px border for ink-muted text when false. Reviewer's reasoning accepted: six stacked 3px edge rules in one tab would make `edge` loud in exactly the way §4 forbids, but a second ad-hoc heading vocabulary at 12px/tracking-wide is not the answer. BINDS T5 — it uses the same variant, never a local label. Cost if wrong: one prop to remove and a heading size to re-pick.
Ruling: CopyButton's variant union narrowed to "default" | "neutral" (not the full four ButtonProps variants) so nothing can pass a second yellow or an accent CTA on a view that already spent its one primary action. Cost if wrong: T5 needs a third variant and widens it by one.
G2 T4 minors (deferred): key={a.id ?? a.title} collides for two null-id rows with the same title; the 6px status square is aria-hidden so list status is colour-only for assistive tech; dueLabel is bare countdown() output where T3's DueNow prefixes "in"; every delete button shares aria-label="delete"; the checklist input has no accessible label; a <button> wrapping <div>s is an invalid content model; with assignments AND gradePicture both empty the grade block renders a rule and a label over nothing; STATUS_FILL mapping / the opacity-80 ungraded row / the focused page's under-24h text-bad countdown have no assertions at all (worth more than the 48h boundary the implementer self-flagged); one loose getAllByText("ungraded") assertion. Also: §6.2 says a "live" countdown and it never ticks — true in T3 and T4 both, flag at T6.
G2 T4: fix round 1/5 (10 addressed, 0 open; 52db8da). S due-window >=0 + overdue fixture; C1 split on /\r?\n\s*\r?\n/ + whitespace-pre-line; C2 Array.isArray + per-item type filter in readChecklist and STATUSES validation in readStatus; I3 key={id}; I4 gap-0 on every tab-panel Card incl. T3's four; I5 PanelLabel deleted -> SectionHeader rule?:boolean (same 11px/0.08em scale, ink-muted when false) used by all 5 focused-page headings + the checklist; I6 order asserted off row buttons; I7 checkbox focus ring + accent-edge; I8 writeChecklist returns boolean + "not saved" note. web 117->130 (22 files). Controller spot-verified S and C1 directly (the re-reviewer's output omitted their verdict lines): AssignmentsTab.tsx:71 has >=0, tests carry hoursAway -351.4 and -24 fixtures, AssignmentPage.tsx:82-83 has the regex + whitespace-pre-line with "A\nB" and "\r\n\r\n" tests. Scoped re-review: all 10 ADDRESSED, no scope creep, and it audited every gap-0 Card — exactly one real regression (the Deadline panel).
G2 T4 live verification (controller, real /api/overview, 1500x1000, meadow-light + violet-dark): list header reads "4 open | 2 due this week | worst: Session 4 Meesho Workbook (10%)" — the 2 proves ruling 10 works live, excluding the -351.4h row. Rows in risk order with "overdue" in bad text, "no due date", "18h" bad-tinted under 48h, ungraded rows; row button carries focus-visible:ring-edge and a click sets the deep-link hash exactly (#assignments/<slug>/<uuid>). Grade bars are the Progress primitive: track bg-card white, fill rgb(232,89,12)=accent, aria-valuenow 0/0/15/80 = 100-aheadPct. Focused page: 1 cta only ("Start in Claude" yellow), "Brief this session" neutral, panel headings measured at 11px / letter-spacing 0.88px (=0.08em) / ink-muted / border 0 — the rule={false} variant is exactly as ruled. 0 middots on both views. Checklist exercised live end to end: add wrote [{"text":"read the case","done":false}], toggle wrote done:true, status wrote "started"; checkbox carries accent-[color:var(--color-edge)] + the focus ring; no spurious "not saved" note. C2 verified live by poisoning storage with '"hello"' and 'bogus-status' then re-navigating — page survives, renders Checklist with 0 items, no white screen. Missing slug/id renders "Assignment not found." + back link.
G2 T4 live findings -> fix round 2 (3):
  L1 Deadline panel spacing MEASURED at 16px / 0px / 8px — the "due Wed 9 Sep 23:59" and "no late submissions" lines render flush. Confirms the side effect the implementer self-flagged and the re-reviewer isolated to this one Card.
  L2 the status square is size-3 with border-[3px], so the fill core is 6px — the row's primary status signal renders as a muddy dot rather than a legible colour. Brief-prescribed sizing, but it does not survive contact with the screen.
  L3 the status segmented control conveys selection only visually (selected = border-edge + bg-card, unselected = transparent) with no aria-pressed, so it has no accessible selected state. Storage read-back is correct.
Ruling: L1 fixed with space-y-2 (8px, on-scale) on the three unmargined Deadline lines — not by reverting the gap-0 convention, which is right and binds T5. Cost if wrong: 8px vs 4px, trivially adjustable.
Ruling: L2 fixed by size-4 (16px, on-scale) so the fill core becomes 10px. Cost if wrong: a slightly larger square than the brief's size-3.
Ruling: L3 fixed with aria-pressed on the segmented control. T2's Settings segmented control has the same gap — already on the deferred a11y minor list for T6, not widened into T4.
G2 T4: fix round 2/5 (3 addressed, 0 open; 2bee1af, folded into the single task commit). L1 the three unmargined Deadline lines wrapped in space-y-2 (gap-0 convention untouched); L2 status square size-3 -> size-4; L3 aria-pressed on the segmented control. web 130->133. Controller re-verified all three LIVE after the fix: Deadline panel gaps now 16px / 8px with 8px inside the stack (was 16/0/8), status square computes 16x16 with a 3px border = a 10px fill core (was 6px), and aria-pressed reads false/true/false with "started" stored. 0 middots.
G2 T4: complete (commit 0341565, review clean, 2 fix rounds). One code commit as the plan requires — the three intermediate commits collapsed, branch unpushed so no shared history rewritten.
G2 T5: Attendance, Exams, Brain, Gaps tabs. NOT started. Inherits three T3/T4 rulings: every tab section is a ui/card.tsx Card WITH gap-0 (children keep their spec §3 margins); panel sub-headings use <SectionHeader rule={false}>, never a local label; the Progress primitive is the only bar (accent fill), never bg-edge. ui/card.tsx must NOT be deleted. CopyButton's variant is narrowed to "default" | "neutral" — widen it only if T5 genuinely needs a third.

G2 T5: Attendance, Exams, Brain, Gaps tabs. Pre-flight scan:
  - All four placeholders already exist at src/components/{AttendanceTab,ExamsTab,BrainTab,GapsTab}.tsx. Ruling: replace in place, keep the TabProps signature (as T3/T4).
  - Brief Step 5 says delete ui/{skeleton,table,card} if unimported. Verified import counts: skeleton 0, table 0, badge 0, alert 0, card 6, progress 1, button 7. Ruling: delete skeleton, table, AND badge + alert — all four are provably unimported after T2's rewrite, and the brief's own principle is "delete the unused ones" (T2's keep-list is stale: Header dropped Badge and App ships its own ErrorCard instead of Alert). card/progress/button stay; card is load-bearing per the T3 ruling. Cost if wrong: two extra vendored files to restore from git.
  - lucide-react: 0 uses in src. Ruling: drop from package.json per brief Step 5.
  - Verified upload(course, kind: "book"|"recording", files: FileList) -> {written, rejected}.
  - gaps.missingBooks[].mentionedIn holds SLUGS but the brief wants course NAMES. No slug->name map is passed. Ruling: build one from ov.brain (it carries course + courseSlug for every course), falling back to the slug when unresolved. Cost if wrong: a slug shows where a name should.
  - chatUnlockAt is a plain number — the brain header's brainReady/chatUnlockAt*100 needs a divide-by-zero guard.
  - Brief Step 2 labels a practice-set button "Practice set · {course}" — MIDDOT, which is the one thing the whole redesign forbids. Ruling: label it "Practice set" and put the course name in a MetaRow beside it, or use "Practice set — {course}" with an em dash. No middot. Cost if wrong: a label reads slightly longer.
G2 T5: implementer DONE (3fe2898). Four tabs + cleanup. web 133->163 (26 files), server 39, check green, npm ci clean.
Ruling (implementer concern 1, resolved from the code): the derived "best case W%" formula is byte-identical to overview.py:575's attendanceBestCase — (tot_att + left_total)/(tot_conf + left_total)*100 — and overallPct matches attendanceNow. Only the zero-guard differs (Python guards on tot_conf, the tab on tot_conf+left_total, diverging only when no session has been held yet: Python 0 vs tab 100). Ruling: the tab reads ov.kpis.attendanceNow and ov.kpis.attendanceBestCase directly instead of recomputing, so the tab and the §5.1 summary chip can never disagree and the guard divergence disappears. "attended X of Y" keeps summing, since kpis carries no counts. Cost if wrong: none, same numbers by construction.
Ruling (implementer concern 2, verified live): its deviation is CORRECT. Live exams carry courses: ["all"] AND courseNames: ["all"], so the brief's `courseNames.length ? join : "all courses"` would render the bare word "all". Checking the ["all"] sentinel is right. Cost if wrong: none, verified against the payload.
New finding from the same live check: an all-course exam's testprepCommands has 17 entries (one per course), so §6.4's "one button per course" renders 17 buttons on a single card — a wall the spec never foresaw. Ruling: when testprepCommands.length > 3, collapse them behind a <details> disclosure labelled with the count; <= 3 render inline. Cost if wrong: one disclosure to unwrap.
G2 T5 review (opus): Spec compliance ❌ — 8 items: rulings 12 and 14 both unimplemented (Attendance still recomputes the percentages, ExamsTab renders all 17 practice-set CTAs inline); §6.5's "Corpus — when 0" renders "0 KB"; §6.6's attendanceStale note is unreachable (nested inside the scrapeStale block and omitted from allEmpty, though _att_stale is independent at overview.py:365) so an attendance-stale-only state renders "Nothing outstanding."; the §6.6 "drop a PDF here" zone has no onDrop/onDragOver so a real drop navigates the browser to the file; the per-group session disclosures render as a detached block after all the group rows; §6.3's "sortable by Now AND by risk" has no way back to risk once Now is clicked; empty mentionedIn is unguarded. Task quality: NEEDS WORK — 2 Critical, 12 Important.
  C1 a failed upload CRASHES the Gaps tab: upload().then has no .catch and no shape guard, the server answers every failure with {error} (index.ts:173-180), so res.written is undefined and result.written.length throws during render — and there is no ErrorBoundary anywhere in src, so the whole tab unmounts.
  C2 empty mentionedIn reaches encodeURIComponent(undefined) -> ?course=undefined -> 400 -> C1's crash; hint renders "courses//inbox/books/".
  I5 the ["all"] sentinel leaks into the Brief-me buttons — courseNames.map over ["all"] emits /mesa:ares-brain-course-brief "all", an invalid command handed to the user. Controller confirmed at ExamsTab.tsx:43-49.
  I10 <summary> has no focus ring. I12 three weak tests: the ExamsTab "interpolated command" test only asserts the absence of "<course>" and would pass with an empty value; two AttendanceTab tests pass unchanged against the T2 placeholder. I13 nothing covers upload failure, mentionedIn: [], or busy disabling Ingest/Transcribe.
  Reviewer confirmed clean: all spacing on-scale, 0 middots, 0 ink-faint, no gradients, edge structural, gap-0 on every Card, no CardDescription, CopyButton union untouched, Progress with accent fill, no mutation of ov.attendance/ov.brain, chatUnlockAt guard present, ruling 9's slug->name map correct, all four deletions verified with 0 remaining imports and npm ci --dry-run exit 0, factories faithful field-for-field, the 1.2:1 tinted-label bug NOT recurred (note on an 8% row computes ~7:1 light / ~6.5:1 dark). Ruling 13 implemented as ruled.
Ruling: the reviewer's Minor about the exam NAME is upgraded to Important — rendering a card's title via SectionHeader rule={false} makes it 11px uppercase ink-muted, quieter than the 13px ink body beneath it, inverting the hierarchy on a whole tab. ui/card.tsx already exports CardTitle (verified). Use it. Cost if wrong: one heading style to re-pick.
G2 T5 minors (deferred): data-book-row exists only for a test; key={i} on a sorted exam list and key={courseName} colliding on duplicate names; the sortable header has no aria-sort and its label never states direction; GapsTab's `courseName` prop actually holds a joined list.
G2 T5 fix round 1: first attempt (agent a61047f) was KILLED by a session rate limit mid-work, leaving GapsTab.tsx uncommitted with C2 + S5 + C1's render half done. Controller verified that partial edit was type-clean with all 163 tests passing, kept it, and re-dispatched for the remainder rather than reverting. Second attempt completed all of C1(finish)/S1-S7/I1-I5 (8a6f225). web 163->176. Implementer concerns: S1 required splitting an existing baseline test that asserted the old recompute behaviour; I1's fix is conditional on isAllCourses rather than a blanket switch to testprepCommands[].course, to preserve the existing "brain not ready shows a course-brief CopyButton" test (which relies on empty testprepCommands with a real non-"all" course); I2 and S6 are pure structural refactors with no fail-then-pass test, declared as such.
G2 T5 scoped re-review: all 14 items ADDRESSED, no new breakage, no scope creep. I1's sentinel leak confirmed closed in every reachable case (an all-courses exam with empty testprepCommands degrades to zero Brief-me buttons rather than falling back to ["all"]). S1's test split judged honest and STRONGER than what it replaced — the old assertion (80/90) was satisfied by both a client recompute and a correct kpis read, so it never proved which path ran; the new one uses deliberately divergent kpis (33/77) to prove ov.kpis is actually read.
G2 T5 live verification (controller, real /api/overview, 1500x1000, meadow-light): Attendance top line reads "attended 32 of 43 | 74% overall | best case 89%" — the 74/89 match ov.kpis exactly, proving S1 landed; header row carries the 3px rule, Now is the accent-coloured sort affordance, row tints render (risk pink / watch amber / on-track clear) with the note as a muted second line on watch+risk only, and pills are AT RISK/WATCH/ON TRACK with black text. Brain: Progress bar + table sorted not-built -> stale, Corpus shows "<1 KB", "1 KB" and "—" (S3 landed). Gaps: each group's disclosure now sits directly beneath its own row (S6 landed). Exams: CardTitle gives a real title (I2 landed), the 17 practice sets ARE collapsed behind "17 practice sets" (S2 landed), and a course-scoped exam correctly shows one yellow "Practice set" + neutral "Brief me".
G2 T5 live findings -> fix round 2 (3):
  L1 an all-course exam renders 17 inline "Brief me" buttons — the same wall S2 was meant to kill, just on the other button set (I1's fix made Brief-me iterate testprepCommands[].course for all-course exams). Worse, all 17 are labelled just "Brief me" with no course name, so they are mutually indistinguishable.
  L2 (raised by the re-reviewer) expanding the practice-set disclosure still shows 17 buttons all at variant="default" (bg-cta). My S2 ruling said collapse and never mentioned recolouring, so this is my under-specification, not the implementer's error.
  L3 the Brain tab's Why column renders a MIDDOT — the server's brain[].reason is literally "6 recordings to transcribe · guide 1 sources behind" (verified in the live payload). The redesign's one absolute rule is no middots, and the web renders one.
Ruling: L1 — Brief-me buttons get the same >3 disclosure treatment as the practice sets, and each button is labelled with its course name so the 17 are distinguishable. Cost if wrong: one more disclosure to unwrap.
Ruling: L2 — a button set is only a cta when there is ONE of it. testprepCommands.length === 1 keeps variant="default"; more than one renders every button variant="neutral". Cost if wrong: the single-course case is the only yellow, which is the intent.
Ruling: L3 — the WEB splits reason on "·" and renders the parts through MetaRow (a 1px vertical rule), which is exactly what MetaRow exists for. Not fixed in scraper/: T5 is forbidden from touching it, the payload string is legitimate data, and presentation is the web's job. Flag for T6 whether overview.py should emit a list instead. Cost if wrong: a rule where a middot was.
G2 T5 fix round 2 (live, c4303c8) + info-tooltip layer (3ef8e18) + fix round 3 (6fc8cc4). Controller re-verified all LIVE:
  L1 fixed — Exams now collapse BOTH sets ("17 practice sets" / "17 briefs"), all details closed by default, and Brief-me buttons are labelled "Brief me — {course}" so the 17 are distinguishable.
  L2 fixed — inside each disclosure all 17 buttons carry 0 bg-cta (verified by class count).
  L3 fixed — Brain "Why" cells that carried a middot now render 1 [role=separator] each with ZERO middots anywhere on the tab ("6 recordings to transcribe | guide 1 sources behind").
  InfoTip verified live: click opens/closes, mouseenter opens, Escape closes, aria-describedby links only while open, panel is 3px edge border + bg-card + box-shadow rgb(15,138,95) 4px 4px 0 + z-30 + p-3, opens downward, exact domain copy renders.
  T1 (tooltip panel was shrink-fitting to 103px against a narrow <th>) fixed — now a definite 260px at both the 78px-wide Floor column and the 336px State column, neither overflowing the viewport, via w-[260px] + an align="right" variant on the last column.
  T2 (the ⓘ button sat INSIDE the <h2>, so every heading's accessible name read "Attendance about Attendance") fixed — h2 now holds the title only with the tip as a sibling, heading accessible name measured as exactly "Attendance", and the border-b-[3px] rule still spans the full 1282px of the card's 1288px inner width. test/App.test.tsx's loosened query restored to the exact { name: "Exams" }.
G2 T5 live finding -> fix round 4 (1): L4 a course-scoped exam card renders TWO yellow CTAs — "Practice set" AND "Brief me — {course}" — because my L2 ruling applied the length===1 rule independently to each button set. Verified on two cards.
Ruling: L4 — "Brief me" is ALWAYS variant="neutral". It is a fallback shown when the brain is not ready, secondary by nature; only "Practice set" is eligible to be the one cta, and only when there is exactly one of them. Cost if wrong: the brief action is never the primary, which is the intent.
Ruling: T5 and the info-tooltip layer ship as ONE commit, not two. I intended two (task + the user's addition), but the tooltip layer edits the same files and in places the same lines as T5's tab bodies (SectionHeader, the four tabs, GradePicture), and the six intermediate commits interleave — so no honest path-based split exists and reordering them by hand would risk the work. The commit message names both pieces so the history stays truthful. Cost if wrong: one commit where a purist would want two.
G2 T5: complete (commit 3f39de3, review clean, 4 fix rounds). Six intermediate commits collapsed into one; branch unpushed so no shared history rewritten.
G2 T6: glue — npm run check, CI, live run both themes x light/dark with screenshots, README "Dashboard" rewrite, docs/lms-api.md note, then the whole-branch review of g-dashboard and merge. NOT started.

G2 T6: glue. Step 1 green from a clean tree: pytest 246, npm ci reifies the lockfile, server vitest 39, web vitest 201, root npm run check exit 0.
Step 2 live run against the PRODUCTION build (npm run --workspace ares-dashboard build + server start on 127.0.0.1:4319, not vite): all six tabs render on live data; focused assignment page opens from a row click with the exact deep-link hash; Settings swaps meadow-light -> violet-light -> violet-dark live with computed tokens confirmed and localStorage written both times; the anti-flash inline script survives the build and is in the served HTML. A sync job from Re-sync ran end to end (running -> done, exit 0, 11 log lines server-side, lastRuns recorded) and the overview refreshed mid-run (attendance 74% -> 75%, chip followed). A transcribe job from Gaps exercised the full job path (accepted, run, lastRuns recorded, surfaced as failed exit 1) but the transcription itself returned needs-manual — media egress still blocked on this machine, as the brief anticipated. Screenshots captured: Today meadow-light, Today violet-dark, focused assignment page, Attendance.
G2 T6 live findings, both fixed in a8e0581: F1 reloading during/after a job left the job-log pane empty FOREVER — App attached streamLog only inside onJob, while the server replays job.log to any new SSE client (index.ts:144, verified by curl returning all 11 lines to a fresh connection); the mount path now re-attaches, with the unmount teardown that was previously missing. F2 ClassTimeline's header was the only Today header without an info tooltip.
G2 T6 observation (not fixed, recorded in docs/lms-api.md): the scraper buffers stdout, so a long sync shows an empty log pane for most of its run then delivers every line at once. The SSE path is fine; running the child unbuffered (-u / PYTHONUNBUFFERED=1) would make the strip genuinely live. Left for a follow-up.
G2 T6 docs: README's Dashboard section rewritten (what each tab answers, the deterministic-job vs copy-command split, themes + ARES_BRAIN_* env vars, how to start and refresh); docs/lms-api.md gained the live-run note. Commits 4e60135 + e6693ce.

G2 T7 (new user request mid-T6): Today tab — a point-format day summary above the timeline, a Rule divider, then the timeline with a per-class "Mindset" button opening a modal. Commit 4814057 + fix b64435c.
Ruling: the modal is FACTS + HAND-OFF, never generated prose. Spec §2 locks the dashboard to zero LLM calls and §11 puts it out of scope, but a real "what to expect / what to prepare" brief is a judgement task. So the modal shows only what the payload knows (time, room, instructor, that session's prereadPaths or an explicit none, what is due in that course, the course's brain state + reason, its attendance standing, meetingLink) under a "What we know" heading, then hands off with a single yellow CopyButton carrying /mesa:ares-brain-course-brief "<course>" and a line saying the written brief comes from Claude Code. No sentence implies the dashboard authored a brief. Cost if wrong: the user wanted generated prose and must paste one command to get it.
Ruling: the Mindset triggers are neutral, not cta — 5-6 classes a day would otherwise be 5-6 yellow buttons and the Today tab's one primary is already spent by Re-sync. Only the in-modal copy command is yellow.
Ruling: a new shared Modal.tsx was built properly (role=dialog, aria-modal, aria-labelledby, focus in on open, focus restored on close, Esc, backdrop click) rather than copying Settings.tsx's pattern, which has a known a11y gap already on the deferred list. Settings was NOT retrofitted — out of scope here; it should adopt Modal later.
G2 T7 live verification: the requested structure renders exactly — "TODAY AT A GLANCE" with real derived points ("5 classes today, 09:30-20:00 (8h in class)", "3 items due today or tomorrow", "Mid Term Exams in 10 days"), a divider, then the timeline with a neutral Mindset button per class. Modal verified live: aria-modal="true", aria-labelledby resolving to the course name, focus lands inside, Esc closes AND restores focus to the trigger, exactly 1 cta, and the copy value is exactly /mesa:ares-brain-course-brief "AI and its Application".
G2 T7 live finding, fixed in b64435c: the modal reintroduced a MIDDOT by printing brain[].reason raw. The split helper existed but was private to BrainTab; it now lives in lib/format.ts as the exported reasonParts and both call sites share it. Re-verified live: 0 middots in the dialog and 0 on the whole page, with 3 Rule separators doing the work.

G2 WHOLE-BRANCH REVIEW (opus, 2ff0094..b64435c, 32 commits): verdict MERGE WITH FIXES. Verified directly: all suites green from clean (231 web / 39 server / 246 pytest, check exit 0, npm ci --dry-run clean); the api.ts <-> overview.py seam diffed programmatically against a LIVE payload — all 18 Overview* interfaces match field-for-field and nullability-for-nullability, zero drift either way, and factories.ts matches too; the CSS matrix parsed from source (@theme top-level only, both dark sets byte-identical modulo indentation, all six combos resolve); contrast ratios recomputed; cta counts taken from the live state histogram.
  MUST-FIX 1: lib/format.ts:66 countdown() UTC-parses a date-only string while fmtDate routes through parseYmdOrIso. ExamsTab.tsx:53 is the one caller passing a bare YYYY-MM-DD, so in Asia/Calcutta the Exams tab renders "9d 18h" for the exam the header chip calls "10d", and an exam happening TODAY renders "overdue" from 05:30 local. Measured against live data. Same local-vs-UTC class as T1 must-fix #1, reintroduced on the web side.
  MUST-FIX 2: calendar-sync landed server-side (KINDS, argv, tests) but was never surfaced in the web — spec §8's folded follow-up says "and surface Sync calendar in the Gaps stale panel and/or Settings". grep -rn calendar-sync dashboard/web/ returns nothing, AND README.md:214 already claims it is reachable from the UI. A dropped clause plus a false doc, on no ruling and no deferred list.
  MUST-FIX 3: Settings.tsx:126-128 hardcodes "attendance minimum 75%" and "chat unlock 15" while ov.attendanceMin / ov.chatUnlockAt are in the payload and both tabs read them correctly. The panel whose entire job is reporting config disagrees with the app the moment either env var changes; it agrees today only by coincidence.
  N1 (Important): cta discipline broken on two whole tabs — BrainTab:100 Ingest and GapsTab:136 Transcribe all are variant="default", which on live data is 17 yellow buttons on Brain and 9 on Gaps, plus the always-visible Re-sync. My own T5 L2/L4 ruling ("a button set is only a cta when there is ONE of it") was applied to Exams and never generalised. This is the exact flat-visual-weight failure the branch exists to fix.
  N2 (Important): brain[].reason round-trips through the banned middot — overview.py:495 does " · ".join(reasons) and lib/format.ts:9 splits it back. The producer HAS the structured parts and throws them away.
  N3 (Important): TodayTab:27 only shows EmptyToday when classes are ALSO empty, so classes-present + nothing-due + nothing-changed (likely right after a clean sync) renders a two-column grid with a dead second column.
  N4 (Important): no ErrorBoundary anywhere in src/ — fetch failure is handled well but any render-time throw on the many nullable payload fields white-screens the whole app.
  N5 (Important): guardOrigin covers GET /api/overview and both mutating routes, but NOT GET /api/state (which returns the job's full log[]) or GET /api/jobs/:id/log. Not exploitable from a plain page (no CORS headers) but DNS rebinding is precisely what the Host check was added to stop.
  Ruling REVERSED on the reviewer's advice: T5 L3 (split the middot in the web) was right for T5, which was forbidden from touching scraper/ — but its own escape hatch, "flag for T6 whether overview.py should emit a list instead", was never exercised. overview.py will emit reasonParts: string[] and the web will stop parsing a delimiter. Cost if wrong: one extra payload field.
  Rulings the reviewer checked and upheld: T4 ruling 10 (hoursAway >= 0), T2 ruling I6 (--color-rule into @theme). It also confirmed the kpis.dueThisWeek name-vs-value mismatch (overview.py:570 computes it from the today/tomorrow list) is still unfixed and unruled — the screen is consistent only because the chip is labelled "due", not "due this week".
  Reviewer's "genuinely good, do not churn": the CSS token matrix (better than the spec's own §4.3 sketch), the api.ts<->overview.py seam, factories.ts, the four T1 data fixes with revert-sensitive tests, Modal.tsx, the T7 no-LLM facts-plus-hand-off ruling, token discipline, and the server's traversal/upload/spawn hardening.
G2 FINAL FIX WAVE (0f2ac21 + 7052960 + b3e6934, then 31b2944): all 12 review items (M1-M8, D1-D4, C1) applied. Scoped re-review: ALL ADDRESSED, no new breakage, no scope creep into the deferred list, and it judged both risky changes legitimate — the sse.test.ts change was a genuine fix (the test never set ARES_BRAIN_DASHBOARD_PORT to its ephemeral port, which the new /api/jobs/:id/log guard exposed; the guard itself is untouched) and the Settings->Modal retrofit is correct with no test weakened.
Controller live re-verification on a fresh production build: all six tabs render, 0 middots on the WHOLE page, cta discipline fixed (Brain 0 yellow, Gaps 0 yellow, Exams 2 = one per course-scoped card). M1 confirmed fixed live — the Exams tab now reads "in 10 days" for Mid Term, exactly matching the header chip's "10d" (was "9d 18h").
G2 final finding, fixed in 31b2944: C1 was INCOMPLETE. The Sync calendar button was added inside the Gaps stale panel, which only renders when scrapeStale || attendanceStale — both false on live data (scrape 1.1h old), so the capability was unreachable from the UI entirely and the README's claim stayed false for the healthy case. Spec §8 offers "the Gaps stale panel and/or Settings".
Ruling: Sync calendar goes in SETTINGS, which is reachable from the sticky summary bar on every tab, under a "Calendar" sub-heading, variant neutral, disabled while busy — the Gaps contextual button stays. You run a calendar sync because deadlines moved, not because your scrape went stale, so it must not be hidden behind a health condition. Verified live: Settings now shows it on every tab, still 0 cta in the panel, still a proper dialog. Cost if wrong: one extra button in Settings.
G2 branch state: 36 commits, pytest 250, web 253/31, server 41/7, root npm run check exit 0, tree clean. Whole-branch review verdict MERGE WITH FIXES -> all fixes applied and re-reviewed clean. NOT YET MERGED — awaiting the user's approval on the merge to main.
