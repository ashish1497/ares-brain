import type { TabProps } from "../lib/types";
import { SectionHeader } from "./SectionHeader";

/** Placeholder — replaced by T5 with per-exam cards and testprep/brief actions. */
export function ExamsTab(_props: TabProps) {
  return <SectionHeader>Exams</SectionHeader>;
}
