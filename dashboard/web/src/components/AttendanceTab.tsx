import type { TabProps } from "../lib/types";
import { SectionHeader } from "./SectionHeader";

/** Placeholder — replaced by T5 with the attendance table. */
export function AttendanceTab(_props: TabProps) {
  return <SectionHeader>Attendance</SectionHeader>;
}
