import type { TabProps } from "../../lib/types";
import { SectionHeader } from "../SectionHeader";

/** Placeholder — replaced by T3 with the class timeline, due-now and changed-feed panels. */
export function TodayTab(_props: TabProps) {
  return <SectionHeader>Today</SectionHeader>;
}
