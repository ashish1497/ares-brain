import type { TabProps } from "../../lib/types";
import { SectionHeader } from "../SectionHeader";

/** Placeholder — replaced by T4 with the assignment list, grade picture and focused page. */
export function AssignmentsTab(_props: TabProps) {
  return <SectionHeader>Assignments</SectionHeader>;
}
