import type { TabProps } from "../lib/types";
import { SectionHeader } from "./SectionHeader";

/** Placeholder — replaced by T5 with the brain-readiness table and ingest/build actions. */
export function BrainTab(_props: TabProps) {
  return <SectionHeader>Brain</SectionHeader>;
}
