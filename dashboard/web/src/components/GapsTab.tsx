import type { TabProps } from "../lib/types";
import { SectionHeader } from "./SectionHeader";

/** Placeholder — replaced by T5 with the transcripts/missing-books/stale panels. */
export function GapsTab(_props: TabProps) {
  return <SectionHeader>Gaps</SectionHeader>;
}
